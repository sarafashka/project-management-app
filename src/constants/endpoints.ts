enum Endpoint {
  BASE_URL = 'https://rss-backend-t43o.onrender.com',
  SIGN_IN = '/signin',
  SIGN_UP = '/signup',
  USERS = '/users',
  BOARDS = '/boards',
  BOARDS_SEARCH = '/search',
  COLUMNS = '/columns',
  TASKS = '/tasks',
}

export default Endpoint;
