enum Endpoint {
  BASE_URL = 'http://185.130.225.43:4000/',
  SIGN_IN = '/signin',
  SIGN_UP = '/signup',
  USERS = '/users',
  BOARDS = '/boards',
  BOARDS_SEARCH = '/search',
  COLUMNS = '/columns',
  TASKS = '/tasks',
}

export default Endpoint;
