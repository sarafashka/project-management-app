enum Endpoint {
  BASE_URL = 'https://rss-g0wt.onrender.com/',
  SIGN_IN = '/signin',
  SIGN_UP = '/signup',
  USERS = '/users',
  BOARDS = '/boards',
  BOARDS_SEARCH = '/search',
  COLUMNS = '/columns',
  TASKS = '/tasks',
}

export default Endpoint;
